/**
 * Notifications module — centralized notification engine.
 *
 * Exports the NotificationService for use by controllers, automation,
 * and event handlers.
 */
const NotificationService = require('./NotificationService');

module.exports = NotificationService;
